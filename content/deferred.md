# Deferred production settings

Only the following business details remain intentionally unresolved:

- **Primary admissions number:** all three current numbers remain available for
  calls and WhatsApp. The existing main CTA continues to use `0678 464 686`
  until the school selects a preferred number.
- **Exact Google Maps location:** the verified written address is published.
  The map link and coordinates remain empty in `src/_data/school.json` until the
  school supplies an exact location.
- **Final custom domain:** the verified current Vercel URL remains the canonical
  base until a domain is purchased.

Do not guess or infer any of these values.
